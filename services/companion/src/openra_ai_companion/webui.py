AI_CONSOLE_HTML = r"""<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="#080b09">
  <title>OpenRA AI - War Room</title>
  <style>
    :root {
      color-scheme: dark;
      --bg:#070908; --surface:#0d110e; --surface-2:#121813; --surface-3:#182019;
      --line:#263128; --line-strong:#3a4b3d; --text:#f2f5ef; --muted:#8f9c91;
      --green:#86f59a; --green-strong:#41d75f; --amber:#f4c95d; --red:#ff705f;
      --cyan:#65d8e6; --blue:#7ba7ff; --shadow:0 22px 70px rgba(0,0,0,.32);
    }
    *{box-sizing:border-box} html{scroll-behavior:smooth} body{margin:0;background:var(--bg);color:var(--text);font:14px/1.45 Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}
    button,input,select{font:inherit} button{color:inherit} a{color:inherit}
    .shell{min-height:100vh;display:grid;grid-template-columns:224px 1fr;background:radial-gradient(circle at 74% -10%,rgba(72,154,84,.14),transparent 32%),linear-gradient(180deg,#080b09,#060807 60%)}
    .sidebar{position:sticky;top:0;height:100vh;border-right:1px solid var(--line);padding:22px 16px;display:flex;flex-direction:column;background:rgba(7,10,8,.9);backdrop-filter:blur(18px);z-index:5}
    .brand{display:flex;align-items:center;gap:11px;padding:2px 7px 25px}.brand-mark{width:36px;height:36px;border:1px solid #4d6c52;border-radius:10px;display:grid;place-items:center;background:linear-gradient(145deg,#18351d,#0c130e);box-shadow:inset 0 0 18px rgba(134,245,154,.08)}
    .radar{width:21px;height:21px;border:1px solid var(--green);border-radius:50%;position:relative}.radar:before,.radar:after{content:"";position:absolute;background:var(--green);opacity:.7}.radar:before{width:1px;height:100%;left:50%}.radar:after{height:1px;width:100%;top:50%}.radar i{position:absolute;width:3px;height:3px;border-radius:50%;background:var(--amber);right:3px;top:4px;box-shadow:0 0 8px var(--amber)}
    .brand strong{display:block;font-size:14px;letter-spacing:.04em}.brand small{color:var(--muted);font:10px/1.2 ui-monospace,monospace;letter-spacing:.1em;text-transform:uppercase}
    nav{display:grid;gap:5px}.nav-button{border:0;background:transparent;border-radius:8px;min-height:43px;padding:0 11px;display:flex;align-items:center;gap:11px;text-align:left;color:#a9b4aa;cursor:pointer;transition:.16s}.nav-button:hover{background:#111712;color:var(--text)}.nav-button.active{background:#172019;color:var(--green);box-shadow:inset 2px 0 var(--green)}
    .nav-icon{width:22px;color:inherit;font:700 11px/1 ui-monospace,monospace;text-align:center}.sidebar-foot{margin-top:auto;border-top:1px solid var(--line);padding:18px 7px 0;color:var(--muted);font-size:11px}.sidebar-foot b{color:#c9d2ca}.keyboard{display:inline-block;border:1px solid var(--line-strong);border-bottom-width:2px;border-radius:4px;padding:2px 5px;color:#cdd6ce;font:10px ui-monospace,monospace;margin-top:8px}
    .workspace{min-width:0}.topbar{height:70px;border-bottom:1px solid var(--line);padding:0 28px;display:flex;align-items:center;justify-content:space-between;gap:18px;position:sticky;top:0;z-index:4;background:rgba(7,10,8,.82);backdrop-filter:blur(18px)}
    .crumb{font:700 11px/1.2 ui-monospace,monospace;color:var(--muted);letter-spacing:.11em;text-transform:uppercase}.crumb span{color:var(--green)}.top-actions{display:flex;align-items:center;gap:9px}.status-pill{display:flex;align-items:center;gap:7px;border:1px solid var(--line);border-radius:999px;background:#0d120e;padding:8px 11px;color:#c0c9c1;font-size:12px;white-space:nowrap}.status-dot{width:7px;height:7px;border-radius:50%;background:var(--amber);box-shadow:0 0 12px currentColor}.status-pill.online .status-dot{background:var(--green)}.status-pill.offline .status-dot{background:var(--red)}
    .icon-button,.button{border:1px solid var(--line-strong);background:#111712;border-radius:7px;min-height:36px;padding:0 12px;display:inline-flex;align-items:center;justify-content:center;gap:7px;cursor:pointer;font-weight:700;font-size:12px}.icon-button:hover,.button:hover{border-color:#608068;background:#182019}.button.primary{background:var(--green);border-color:var(--green);color:#071008}.button:disabled{opacity:.5;cursor:wait}
    main{width:min(1440px,100%);margin:0 auto;padding:26px 28px 70px}.view{display:none}.view.active{display:block}.view-head{display:flex;justify-content:space-between;align-items:flex-end;gap:20px;margin-bottom:20px}.eyebrow{font:700 10px/1.2 ui-monospace,monospace;letter-spacing:.14em;text-transform:uppercase;color:var(--green)}h1{font-size:clamp(27px,4vw,42px);line-height:1.05;letter-spacing:-.035em;margin:7px 0 7px}h2{font-size:16px;margin:0}h3{font-size:13px;margin:0}.lede,.sub{margin:0;color:var(--muted)}.lede{max-width:650px;font-size:14px}.sub{font-size:12px;margin-top:4px}
    .grid{display:grid;gap:14px}.metrics{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:14px}.metric,.card{border:1px solid var(--line);background:linear-gradient(145deg,rgba(18,24,19,.98),rgba(11,15,12,.98));border-radius:10px;box-shadow:var(--shadow)}.metric{min-height:126px;padding:17px;position:relative;overflow:hidden}.metric:after{content:"";position:absolute;right:-22px;bottom:-32px;width:92px;height:92px;border:1px solid rgba(134,245,154,.08);border-radius:50%}.metric-label{font:700 10px/1.2 ui-monospace,monospace;color:var(--muted);letter-spacing:.1em;text-transform:uppercase}.metric-value{font-size:29px;font-weight:760;letter-spacing:-.04em;margin:9px 0 2px}.metric-detail{color:var(--muted);font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.metric.good .metric-value{color:var(--green)}.metric.warn .metric-value{color:var(--amber)}.metric.danger .metric-value{color:var(--red)}.meter{height:4px;border-radius:5px;background:#222b24;margin-top:13px;overflow:hidden}.meter i{display:block;height:100%;border-radius:inherit;background:var(--green)}.warn .meter i{background:var(--amber)}.danger .meter i{background:var(--red)}
    .card{padding:18px}.card-head{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;margin-bottom:15px}.card-kicker{font:700 9px/1.2 ui-monospace,monospace;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:5px}.badge{border:1px solid var(--line-strong);border-radius:999px;padding:4px 8px;color:var(--muted);font:700 9px/1 ui-monospace,monospace;text-transform:uppercase}.badge.good{color:var(--green);border-color:#376741;background:rgba(65,215,95,.07)}.badge.warn{color:var(--amber);border-color:#66562d}.badge.danger{color:var(--red);border-color:#6f3c34}
    .live-grid{grid-template-columns:minmax(0,1.22fr) minmax(320px,.78fr)}.stack{display:grid;gap:14px}.decision-chain{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.decision-step{border:1px solid var(--line);background:#0b0f0c;border-radius:8px;padding:13px;min-height:128px;position:relative}.decision-step:not(:last-child):after{content:"";position:absolute;width:9px;height:1px;right:-9px;top:24px;background:var(--line-strong)}.step-num{font:700 9px ui-monospace,monospace;color:var(--green);letter-spacing:.08em}.decision-step h3{margin:7px 0 5px;text-transform:uppercase;font:700 10px ui-monospace,monospace;letter-spacing:.08em}.decision-step p{color:#b5c0b6;font-size:11px;margin:0;display:-webkit-box;-webkit-line-clamp:4;-webkit-box-orient:vertical;overflow:hidden}
    .timeline{display:grid}.timeline-row{display:grid;grid-template-columns:70px 12px 1fr;gap:9px;min-height:58px;cursor:pointer}.timeline-row:hover .timeline-copy{color:var(--text)}.timeline-tick{font:10px ui-monospace,monospace;color:var(--muted);padding-top:3px}.timeline-track{position:relative}.timeline-track:before{content:"";position:absolute;left:5px;top:10px;bottom:-10px;width:1px;background:var(--line)}.timeline-row:last-child .timeline-track:before{display:none}.timeline-node{position:absolute;top:4px;left:1px;width:9px;height:9px;border:2px solid var(--green);border-radius:50%;background:var(--surface)}.timeline-node.orders{border-color:var(--cyan)}.timeline-node.event{border-color:var(--amber)}.timeline-node.round_review{border-color:var(--red)}.timeline-copy{padding:0 0 13px;color:#c2cbc3;font-size:12px}.timeline-copy b{display:block;color:var(--text);font-size:11px;margin-bottom:2px}.timeline-copy small{color:var(--muted)}
    .frame{border:1px solid var(--line);border-radius:8px;background:#050706;min-height:230px;display:grid;place-items:center;overflow:hidden;position:relative}.frame img{display:block;width:100%;height:100%;max-height:430px;object-fit:contain;image-rendering:pixelated}.frame-empty{padding:35px;text-align:center;color:var(--muted)}.frame-meta{position:absolute;left:9px;bottom:9px;border:1px solid rgba(255,255,255,.12);background:rgba(5,8,6,.82);border-radius:5px;padding:5px 7px;color:#cbd4cc;font:9px ui-monospace,monospace}.frame-strip{display:flex;gap:6px;margin-top:8px;overflow:auto;padding-bottom:2px}.frame-thumb{flex:0 0 72px;height:48px;border:1px solid var(--line);border-radius:5px;background:#080b09;overflow:hidden;cursor:pointer;padding:0}.frame-thumb:hover,.frame-thumb.active{border-color:var(--green)}.frame-thumb img{width:100%;height:100%;object-fit:cover;image-rendering:pixelated}
    .review-grid{grid-template-columns:minmax(0,1.1fr) minmax(300px,.9fr)}.score-row{display:grid;grid-template-columns:1fr auto;gap:10px;align-items:center;margin:11px 0}.score-row label{color:#b9c3ba;font-size:12px}.score-bar{grid-column:1/-1;height:7px;background:#202922;border-radius:5px;overflow:hidden}.score-bar i{display:block;height:100%;background:linear-gradient(90deg,var(--green-strong),var(--green));border-radius:inherit}.score-bar.red i{background:var(--red)}.score-bar.amber i{background:var(--amber)}
    .assessment{display:grid;grid-template-columns:1fr 1fr;gap:10px}.assessment-box{border:1px solid var(--line);border-radius:8px;background:#0a0e0b;padding:13px}.assessment-box h3{font:700 10px ui-monospace,monospace;text-transform:uppercase;letter-spacing:.08em;margin-bottom:9px}.assessment-box.good h3{color:var(--green)}.assessment-box.warn h3{color:var(--amber)}.assessment-box ul{margin:0;padding:0;list-style:none;display:grid;gap:8px}.assessment-box li{font-size:11px;color:#b7c1b8;padding-left:14px;position:relative}.assessment-box li:before{content:"";position:absolute;left:0;top:6px;width:5px;height:5px;border-radius:50%;background:currentColor}
    .run-chart{height:190px;border:1px solid var(--line);border-radius:8px;background:linear-gradient(180deg,rgba(29,41,31,.35),transparent),repeating-linear-gradient(0deg,transparent,transparent 37px,rgba(255,255,255,.035) 38px);padding:15px 12px 24px;display:flex;align-items:flex-end;gap:7px}.run-column{height:100%;flex:1;min-width:16px;display:flex;flex-direction:column;justify-content:flex-end;align-items:stretch;gap:5px;position:relative}.run-bar{min-height:8px;border-radius:3px 3px 1px 1px;background:var(--red);opacity:.76;transition:.2s}.run-column.win .run-bar{background:var(--green)}.run-column:hover .run-bar{opacity:1}.run-label{text-align:center;color:var(--muted);font:8px ui-monospace,monospace;position:absolute;top:calc(100% + 7px);width:100%}.run-tip{display:none;position:absolute;bottom:calc(100% + 6px);left:50%;transform:translateX(-50%);z-index:2;background:#050706;border:1px solid var(--line-strong);border-radius:5px;padding:6px;white-space:nowrap;font:9px ui-monospace,monospace}.run-column:hover .run-tip{display:block}
    .difficulty-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(145px,1fr));gap:8px}.difficulty{border:1px solid var(--line);border-radius:8px;padding:12px;background:#0a0e0b}.difficulty b{font-size:20px;color:var(--green)}.difficulty span{display:block;color:var(--muted);font:9px ui-monospace,monospace;text-transform:uppercase;letter-spacing:.08em}
    .table-wrap{overflow:auto;border:1px solid var(--line);border-radius:8px}table{width:100%;border-collapse:collapse;min-width:720px}th,td{text-align:left;padding:10px 12px;border-bottom:1px solid var(--line);font-size:11px}th{background:#0b0f0c;color:var(--muted);font:700 9px ui-monospace,monospace;text-transform:uppercase;letter-spacing:.08em}tbody tr{cursor:pointer}tbody tr:hover{background:#141a15}tbody tr:last-child td{border-bottom:0}.result{font-weight:800}.result.win{color:var(--green)}.result.loss{color:var(--red)}
    .brain-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.brain-card{border:1px solid var(--line);border-radius:8px;background:#0a0e0b;padding:14px;min-height:120px}.brain-card b{display:block;margin:8px 0 4px;font-size:17px}.brain-card p{margin:0;color:var(--muted);font-size:11px}.goal-list{display:grid;gap:8px}.goal{display:grid;grid-template-columns:9px 1fr auto;gap:9px;align-items:start;border:1px solid var(--line);border-radius:7px;padding:10px;background:#0a0e0b}.goal-dot{width:7px;height:7px;border-radius:50%;background:var(--cyan);margin-top:4px}.goal small{color:var(--muted)}
    details{border:1px solid var(--line);border-radius:8px;background:#090c0a}summary{padding:11px 13px;cursor:pointer;color:#bac4bb;font-weight:700;font-size:11px}pre{margin:0;border-top:1px solid var(--line);padding:14px;max-height:360px;overflow:auto;white-space:pre-wrap;color:#9fb0a1;font:10px/1.55 ui-monospace,monospace}
    .settings-grid{grid-template-columns:minmax(0,1.1fr) minmax(320px,.9fr)}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field{display:grid;gap:6px}.field.full{grid-column:1/-1}.field label{color:var(--muted);font:700 9px ui-monospace,monospace;letter-spacing:.08em;text-transform:uppercase}.field input,.field select{width:100%;height:40px;border:1px solid var(--line-strong);border-radius:7px;background:#090d0a;color:var(--text);padding:0 10px;outline:none}.field input:focus,.field select:focus{border-color:var(--green)}.actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:15px}.diagnostics{display:grid;grid-template-columns:1fr 1fr;gap:8px}.diag-output{min-height:160px;border:1px solid var(--line);border-radius:7px;background:#070a08;padding:12px;margin-top:10px;color:#aab7ac;font:10px/1.5 ui-monospace,monospace;white-space:pre-wrap;overflow:auto}
    .empty{min-height:170px;display:grid;place-items:center;text-align:center;color:var(--muted);padding:25px}.empty b{display:block;color:#c9d2ca;margin-bottom:5px}.toast{position:fixed;right:18px;bottom:18px;max-width:360px;border:1px solid var(--line-strong);border-radius:8px;background:#111712;padding:11px 14px;box-shadow:var(--shadow);transform:translateY(30px);opacity:0;pointer-events:none;transition:.2s;z-index:20}.toast.show{transform:none;opacity:1}.select{height:36px;border:1px solid var(--line-strong);border-radius:7px;background:#0d120e;color:var(--text);padding:0 9px;max-width:320px}
    @media(max-width:1080px){.metrics{grid-template-columns:repeat(2,1fr)}.live-grid,.review-grid,.settings-grid{grid-template-columns:1fr}.decision-chain{grid-template-columns:repeat(2,1fr)}.decision-step:after{display:none}.brain-grid{grid-template-columns:1fr 1fr}}
    @media(max-width:720px){.shell{display:block}.sidebar{position:fixed;left:0;right:0;bottom:0;top:auto;width:auto;height:62px;padding:6px 8px;border:0;border-top:1px solid var(--line);z-index:10}.brand,.sidebar-foot{display:none}nav{display:grid;grid-template-columns:repeat(5,1fr);gap:2px}.nav-button{min-height:49px;display:grid;place-items:center;gap:1px;padding:3px;font-size:9px;text-align:center}.nav-button.active{box-shadow:inset 0 2px var(--green)}.nav-icon{font-size:9px}.topbar{height:60px;padding:0 14px}.status-pill span{display:none}.top-actions .status-pill:nth-child(2){display:none}main{padding:20px 14px 90px}.view-head{align-items:flex-start;flex-direction:column}.metrics{grid-template-columns:1fr 1fr}.metric{min-height:112px;padding:14px}.metric-value{font-size:24px}.decision-chain,.assessment,.brain-grid,.form-grid{grid-template-columns:1fr}.card{padding:14px}.top-actions .icon-button{padding:0 9px}.crumb{font-size:9px}}
  </style>
</head>
<body>
<div class="shell">
  <aside class="sidebar">
    <div class="brand"><div class="brand-mark"><span class="radar"><i></i></span></div><div><strong>AI WAR ROOM</strong><small>Companion Console</small></div></div>
    <nav aria-label="War Room sections">
      <button class="nav-button active" data-tab="live"><span class="nav-icon">LIVE</span><span>Command</span></button>
      <button class="nav-button" data-tab="review"><span class="nav-icon">AAR</span><span>After Action</span></button>
      <button class="nav-button" data-tab="learning"><span class="nav-icon">AI</span><span>Learning</span></button>
      <button class="nav-button" data-tab="brain"><span class="nav-icon">SYS</span><span>Brain</span></button>
      <button class="nav-button" data-tab="settings"><span class="nav-icon">CFG</span><span>Settings</span></button>
    </nav>
    <div class="sidebar-foot"><b>Local and private</b><br>Battle data never leaves your machine unless your configured model route requires it.<br><span class="keyboard">CTRL + SPACE</span></div>
  </aside>
  <section class="workspace">
    <header class="topbar">
      <div class="crumb">OPENRA AI / <span id="crumb">LIVE COMMAND</span></div>
      <div class="top-actions">
        <div id="router_status" class="status-pill"><i class="status-dot"></i><span>Connecting</span></div>
        <div id="mode_status" class="status-pill"><i class="status-dot"></i><span>Auto unknown</span></div>
        <button id="refresh" class="icon-button" title="Refresh all War Room data">REFRESH</button>
      </div>
    </header>
    <main>
      <section id="view-live" class="view active">
        <div class="view-head"><div><div class="eyebrow">Real-time operational picture</div><h1>Live Command</h1><p class="lede">See what the assistant knows, why it is acting, and whether the action worked.</p></div><div class="badge" id="live_sync">Waiting for sync</div></div>
        <div id="live_metrics" class="grid metrics"></div>
        <div class="grid live-grid">
          <div class="stack">
            <section class="card"><div class="card-head"><div><div class="card-kicker">Decision trace</div><h2>Observe to verified outcome</h2><p class="sub">The current reasoning chain, expressed as game facts and receipts.</p></div><span class="badge" id="chain_tick">No snapshot</span></div><div id="decision_chain" class="decision-chain"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">Activity</div><h2>Operational timeline</h2></div><span class="badge">Click to inspect frame</span></div><div id="live_timeline" class="timeline"></div></section>
          </div>
          <div class="stack">
            <section class="card"><div class="card-head"><div><div class="card-kicker">Fog-respecting visual evidence</div><h2>What the AI saw</h2></div><span id="frame_reason" class="badge">Recorded</span></div><div id="live_frame"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">Current intent</div><h2>Strategy and goals</h2></div></div><div id="live_intent"></div></section>
          </div>
        </div>
      </section>

      <section id="view-review" class="view">
        <div class="view-head"><div><div class="eyebrow">After-action review</div><h1>Match Debrief</h1><p class="lede">Replay decisions, inspect evidence, and connect battlefield outcomes to the next learning change.</p></div><select id="attempt_select" class="select" aria-label="Select recorded match"></select></div>
        <div id="review_metrics" class="grid metrics"></div>
        <div class="grid review-grid">
          <div class="stack">
            <section class="card"><div class="card-head"><div><div class="card-kicker">Performance</div><h2>Outcome scorecard</h2></div><span id="review_result" class="badge">No match</span></div><div id="scorecard"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">Causal record</div><h2>Decision timeline</h2></div><span class="badge">Orders + events</span></div><div id="review_timeline" class="timeline"></div></section>
          </div>
          <div class="stack">
            <section class="card"><div class="card-head"><div><div class="card-kicker">Tactical frames</div><h2>Evidence scrubber</h2></div><span id="review_frame_count" class="badge">0 frames</span></div><div id="review_frame"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">System assessment</div><h2>Keep and change</h2></div></div><div id="assessment" class="assessment"></div></section>
          </div>
        </div>
      </section>

      <section id="view-learning" class="view">
        <div class="view-head"><div><div class="eyebrow">Persistent improvement loop</div><h1>Learning Lab</h1><p class="lede">Track performance across attempts and see which lessons become candidate or promoted policy.</p></div><span id="active_policy" class="badge good">Policy: baseline</span></div>
        <div id="learning_metrics" class="grid metrics"></div>
        <div class="grid review-grid">
          <div class="stack">
            <section class="card"><div class="card-head"><div><div class="card-kicker">Attempt history</div><h2>Performance trend</h2><p class="sub">Green columns are verified wins. Height represents combat-value efficiency.</p></div></div><div id="run_chart" class="run-chart"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">Comparison</div><h2>Recent attempts</h2></div></div><div id="attempt_table" class="table-wrap"></div></section>
          </div>
          <div class="stack">
            <section class="card"><div class="card-head"><div><div class="card-kicker">Difficulty ladder</div><h2>Verified capability</h2></div></div><div id="difficulty_grid" class="difficulty-grid"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">Latest lessons</div><h2>What the system learned</h2></div></div><div id="learning_lessons" class="assessment"></div></section>
            <section class="card"><div class="card-head"><div><div class="card-kicker">Policy gates</div><h2>Promotion status</h2></div></div><div id="policy_gates"></div></section>
          </div>
        </div>
      </section>

      <section id="view-brain" class="view">
        <div class="view-head"><div><div class="eyebrow">Control architecture</div><h1>Brain Inspector</h1><p class="lede">Understand which layer owns the next action: player, mission logic, native controller, or strategic model.</p></div><span id="brain_owner" class="badge">Owner unknown</span></div>
        <div id="brain_cards" class="grid brain-grid"></div>
        <div class="grid review-grid" style="margin-top:14px">
          <section class="card"><div class="card-head"><div><div class="card-kicker">Goal blackboard</div><h2>Active and recently verified goals</h2></div></div><div id="goals" class="goal-list"></div></section>
          <section class="card"><div class="card-head"><div><div class="card-kicker">Decision ownership</div><h2>Leases and receipts</h2></div></div><div id="leases" class="goal-list"></div></section>
        </div>
        <details style="margin-top:14px"><summary>Raw brain state for engineering inspection</summary><pre id="brain_raw">No brain state loaded.</pre></details>
      </section>

      <section id="view-settings" class="view">
        <div class="view-head"><div><div class="eyebrow">Local control plane</div><h1>Settings and Diagnostics</h1><p class="lede">Choose the brain routes and verify the same text, microphone, and speech paths used during gameplay.</p></div><a class="button" href="http://127.0.0.1:8788/">EARTH MISSION STUDIO</a></div>
        <div class="grid settings-grid">
          <section class="card"><div class="card-head"><div><div class="card-kicker">Routing</div><h2>AI layer configuration</h2></div><span class="badge">Keys never displayed</span></div>
            <div class="form-grid">
              <div class="field full"><label for="router_url">AI layer URL</label><input id="router_url" autocomplete="off"></div>
              <div class="field"><label for="text_model">Companion model</label><input id="text_model"></div>
              <div class="field"><label for="transcribe_model">Transcription route</label><input id="transcribe_model"></div>
              <div class="field"><label for="transcribe_language">Speech language</label><input id="transcribe_language" maxlength="5"></div>
              <div class="field"><label for="speech_voice">Voice</label><input id="speech_voice"></div>
              <div class="field"><label for="speech_model">Speech route</label><input id="speech_model"></div>
              <div class="field"><label for="timeout_seconds">Timeout seconds</label><input id="timeout_seconds" type="number" min="1" max="120"></div>
              <div class="field full"><label for="native_strategy">Delegated OpenRA strategy</label><select id="native_strategy"><option value="adaptive">Adaptive - AI directs native profiles</option><option value="normal">Balanced combined arms</option><option value="rush">Aggressive pressure</option><option value="turtle">Fortified defense</option><option value="naval">Naval control</option><option value="medium">Measured pressure</option></select></div>
            </div>
            <div class="actions"><button class="button primary" id="save">SAVE CONFIGURATION</button><button class="button" id="reload">RELOAD</button></div>
          </section>
          <section class="card"><div class="card-head"><div><div class="card-kicker">End-to-end checks</div><h2>Route diagnostics</h2></div></div>
            <div class="diagnostics"><button class="button" data-test="connection">1. CONNECTION</button><button class="button" data-test="chat">2. TEXT MODEL</button><button class="button" data-test="microphone">3. MICROPHONE</button><button class="button" data-test="speech">4. SPEECH</button></div>
            <div class="actions"><button class="button primary" data-test="full">RUN FULL AI TEST</button></div><div id="diag_output" class="diag-output">Ready.</div>
          </section>
        </div>
      </section>
    </main>
  </section>
</div>
<div id="toast" class="toast"></div>
<script>
const fields=['router_url','text_model','transcribe_model','transcribe_language','speech_model','speech_voice','timeout_seconds','native_strategy'];
const app={health:{},state:{},dashboard:{},matches:[],latest:{},selected:null,tab:'live',frameIndex:-1,lastSync:null};
const $=selector=>document.querySelector(selector);
const $$=selector=>[...document.querySelectorAll(selector)];
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const num=value=>Number(value||0).toLocaleString();
const cap=value=>String(value||'unknown').replace(/[_-]+/g,' ').replace(/\b\w/g,char=>char.toUpperCase());
const clamp=(value,min=0,max=100)=>Math.max(min,Math.min(max,Number(value)||0));
async function request(path,options={}){const response=await fetch(path,{headers:{'Content-Type':'application/json'},...options});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.detail||data.error||`HTTP ${response.status}`);return data}
function toast(message){const node=$('#toast');node.textContent=message;node.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>node.classList.remove('show'),2600)}
function metric(label,value,detail='',tone='',bar=null){return `<article class="metric ${tone}"><div class="metric-label">${esc(label)}</div><div class="metric-value">${esc(value)}</div><div class="metric-detail">${esc(detail)}</div>${bar===null?'':`<div class="meter"><i style="width:${clamp(bar)}%"></i></div>`}</article>`}
function empty(title,detail){return `<div class="empty"><div><b>${esc(title)}</b>${esc(detail)}</div></div>`}
function resultClass(record){return record?.won?'good':String(record?.result||'').toLowerCase()==='incomplete'?'warn':'danger'}
function resultLabel(record){return record?.won?'VERIFIED WIN':cap(record?.result||'No result')}
function finalEconomy(record){return record?.resources?.final_economy||{}}
function finalMilitary(record){return record?.resources?.final_military||{}}
function frameUrl(record,frame){if(!record?.attempt_id||!frame?.file)return'';const filename=String(frame.file).split('/').pop();return `/v1/learning/matches/${encodeURIComponent(record.attempt_id)}/frames/${encodeURIComponent(filename)}`}
function describeOrders(item){const orders=item?.orders||[];if(!orders.length)return'No orders recorded.';return orders.slice(0,4).map(order=>`${cap(order.action)}${order.item?' '+order.item:''}${order.target_actor?' target '+order.target_actor:''}`).join(' / ')}
function timelineTitle(item){if(item.type==='decision')return item.decision||'Strategic decision';if(item.type==='orders')return describeOrders(item);if(item.type==='event')return cap(item.event||'Game event');return `Decision round ${item.round||0}`}
function timelineDetail(item){if(item.type==='decision')return item.evidence||item.expected_result||'';if(item.type==='orders')return `${item.orders?.length||0} order(s) issued`;if(item.type==='event'){const military=item.military||{};return `Army ${num(military.army_value)} / kills ${num(military.units_killed)}`;}return item.error||item.note||'Round reviewed';}
function timelineHtml(record,limit=14){const timeline=(record?.timeline||[]).slice(-limit).reverse();if(!timeline.length)return empty('No recorded activity','Start an autonomous match to populate the causal timeline.');return timeline.map(item=>`<div class="timeline-row" data-tick="${Number(item.tick)||0}"><div class="timeline-tick">T+${num(item.tick)}</div><div class="timeline-track"><i class="timeline-node ${esc(item.type)}"></i></div><div class="timeline-copy"><b>${esc(timelineTitle(item))}</b><span>${esc(timelineDetail(item)).slice(0,210)}</span></div></div>`).join('')}
function assessmentHtml(assessment={}){const strengths=assessment.strengths||[],improvements=assessment.improvements||[];return `<div class="assessment-box good"><h3>Keep doing</h3>${strengths.length?`<ul>${strengths.map(item=>`<li>${esc(item)}</li>`).join('')}</ul>`:'<span class="sub">No verified strengths yet.</span>'}</div><div class="assessment-box warn"><h3>Change next</h3>${improvements.length?`<ul>${improvements.map(item=>`<li>${esc(item)}</li>`).join('')}</ul>`:'<span class="sub">No corrective lesson recorded.</span>'}</div>`}
function frameViewer(record,targetId){const evidence=record?.visual_evidence||{};const frames=evidence.recent_frames||[];if(!frames.length){$(targetId).innerHTML=empty('No tactical frame available','Frames are captured every five seconds and at important decisions.');return}if(app.frameIndex<0||app.frameIndex>=frames.length)app.frameIndex=frames.length-1;const frame=frames[app.frameIndex];$(targetId).innerHTML=`<div class="frame"><img src="${esc(frameUrl(record,frame))}" alt="Fog-respecting tactical frame at tick ${Number(frame.tick)||0}"><span class="frame-meta">T+${num(frame.tick)} / ${esc(frame.reason||'evidence')}</span></div><div class="frame-strip">${frames.slice(-8).map((item,index)=>{const actual=frames.length-Math.min(8,frames.length)+index;return `<button class="frame-thumb ${actual===app.frameIndex?'active':''}" data-frame="${actual}" title="Tick ${Number(item.tick)||0}"><img src="${esc(frameUrl(record,item))}" alt="Frame at tick ${Number(item.tick)||0}"></button>`}).join('')}</div>`}
function selectFrameNear(tick){const record=app.selected||app.latest;const frames=record?.visual_evidence?.recent_frames||[];if(!frames.length)return;let best=0,distance=Infinity;frames.forEach((frame,index)=>{const delta=Math.abs((Number(frame.tick)||0)-tick);if(delta<distance){distance=delta;best=index}});app.frameIndex=best;frameViewer(record,app.tab==='review'?'#review_frame':'#live_frame')}
function renderShell(){const reachable=!!app.health?.router?.reachable;const router=$('#router_status');router.className=`status-pill ${reachable?'online':'offline'}`;router.querySelector('span').textContent=reachable?'AI layer online':'AI layer unavailable';const auto=!!app.state.auto_act_enabled;const mode=$('#mode_status');mode.className=`status-pill ${auto?'online':''}`;mode.querySelector('span').textContent=auto?'Auto assistant on':'Assisted mode';$('#live_sync').textContent=app.lastSync?`SYNCED ${app.lastSync.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'})}`:'WAITING FOR SYNC'}
function renderLive(){const snapshot=app.state.snapshot;const threat=app.state.threat||app.health.threat||{};const brain=app.state.brain||{};const latest=app.latest||{};const economy=snapshot?.economy||finalEconomy(latest);const military=snapshot?.military||finalMilitary(latest);const map=snapshot?.map||latest.map||'No active match';const score=Number(threat.score)||0;$('#live_metrics').innerHTML=[metric('Threat',score,threat.reason||'No immediate visible threat',score>=70?'danger':score>=35?'warn':'good',score),metric('Map',map,snapshot?`Tick ${num(snapshot.tick)}`:'Latest recorded battlefield'),metric('Army value',num(military.army_value),`${num(military.units_killed)} kills / ${num(military.units_lost)} losses`,military.army_value?'good':''),metric('Economy',num((economy.cash||0)+(economy.ore||0)),`${num(economy.harvesters)} harvesters / ${num(economy.power_balance)} power`,economy.storage_percent>80?'warn':'',economy.storage_percent||0)].join('');
  const timeline=latest.timeline||[];const decision=[...timeline].reverse().find(item=>item.type==='decision');const orders=[...timeline].reverse().find(item=>item.type==='orders');const update=(brain.latest_goal_updates||[]).at(-1);const controller=brain.controller||{};const observation=snapshot?`${num(snapshot.own_units)} units, ${num(snapshot.visible_enemies?.length)} visible enemies, ${num(snapshot.explored_percent)}% explored.`:`Latest match: ${latest.map||'none'} versus ${latest.opponent||'unknown'}.`;$('#chain_tick').textContent=snapshot?`LIVE TICK ${num(snapshot.tick)}`:`RECORDED TICK ${num(latest.tick)}`;$('#decision_chain').innerHTML=[['01','Observe',observation],['02','Reason',decision?.decision||controller.next_fast_decision?.summary||'Waiting for a strategic decision.'],['03','Act',orders?describeOrders(orders):app.state.pending_action?.summary||'No action is currently pending.'],['04','Verify',update?`${cap(update.status)}: ${update.summary||update.instruction||'Goal receipt updated.'}`:latest.won?'Engine verified the match win.':'Awaiting a verified receipt.']].map(step=>`<article class="decision-step"><span class="step-num">${step[0]}</span><h3>${step[1]}</h3><p>${esc(step[2])}</p></article>`).join('');$('#live_timeline').innerHTML=timelineHtml(latest,12);frameViewer(latest,'#live_frame');const strategy=app.health.strategy||{};const goals=brain.goals?.active||[];$('#live_intent').innerHTML=`<div class="brain-card"><div class="card-kicker">Brain owner</div><b>${esc(cap(brain.owner||'waiting'))}</b><p>${esc(strategy.intent||'Waiting for a live strategy program.')}</p></div><div class="goal-list" style="margin-top:8px">${goals.length?goals.slice(0,4).map(goal=>`<div class="goal"><i class="goal-dot"></i><div><b>${esc(goal.summary||goal.instruction||'Active goal')}</b><small>${esc(cap(goal.scope||goal.owner||''))}</small></div><span class="badge">${esc(cap(goal.status||'active'))}</span></div>`).join(''):empty('No active goals',snapshot?'The controller is between verified actions.':'Enter a match to begin live goal tracking.')}</div>`}
function selectedRecord(){return app.selected||app.matches[0]||app.latest||{}}
function renderReview(){const record=selectedRecord();const military=finalMilitary(record),economy=finalEconomy(record),ratio=Number(record?.resources?.combat_value_ratio)||0;$('#attempt_select').innerHTML=app.matches.length?app.matches.map(item=>`<option value="${esc(item.attempt_id)}" ${item.attempt_id===record.attempt_id?'selected':''}>${item.won?'WIN':'LOSS'} - ${esc(item.map)} / ${esc(item.opponent)}</option>`).join(''):'<option>No recorded matches</option>';$('#review_metrics').innerHTML=[metric('Result',resultLabel(record),`${cap(record.opponent)} / ${record.map||'unknown'}`,resultClass(record)),metric('Combat ratio',ratio.toFixed(2),`${num(military.kills_cost)} destroyed / ${num(military.deaths_cost)} lost`,ratio>=1?'good':'danger',clamp(ratio*50)),metric('Structures',num(military.buildings_killed),`${num(military.buildings_lost)} friendly lost`,military.buildings_lost?'danger':'good'),metric('Decision rounds',num(record.rounds),`${num(record.decisions?.orders)} orders logged`)].join('');const result=$('#review_result');result.className=`badge ${resultClass(record)}`;result.textContent=resultLabel(record);const max=Math.max(1,military.units_killed||0,military.units_lost||0,military.buildings_killed||0,record.resources?.peak_harvesters||0);$('#scorecard').innerHTML=[['Enemy units destroyed',military.units_killed||0,''],['Friendly units lost',military.units_lost||0,'red'],['Enemy structures destroyed',military.buildings_killed||0,'amber'],['Peak harvesters',record.resources?.peak_harvesters||economy.harvesters||0,'']].map(([label,value,tone])=>`<div class="score-row"><label>${esc(label)}</label><b>${num(value)}</b><div class="score-bar ${tone}"><i style="width:${clamp(value/max*100)}%"></i></div></div>`).join('');$('#review_timeline').innerHTML=timelineHtml(record,30);$('#review_frame_count').textContent=`${num(record.visual_evidence?.frame_count)} FRAMES`;frameViewer(record,'#review_frame');$('#assessment').innerHTML=assessmentHtml(record.assessment||{})}
function renderLearning(){const data=app.dashboard||{};const records=[...app.matches].reverse();$('#active_policy').textContent=`POLICY: ${cap(data.policies?.active_policy||'baseline')}`;$('#learning_metrics').innerHTML=[metric('Attempts',num(data.attempts),`${num(data.losses_or_incomplete)} losses or incomplete`),metric('Verified wins',num(data.wins),'Engine terminal results','good'),metric('Win rate',`${num(data.win_rate)}%`,'Across all recorded attempts',data.win_rate>=50?'good':'warn',data.win_rate),metric('Safety violations',num(app.matches.reduce((sum,item)=>sum+(Number(item.safety_violations)||0),0)),'Required for policy promotion',app.matches.some(item=>item.safety_violations)?'danger':'good')].join('');if(records.length){const maxRatio=Math.max(1,...records.map(item=>Number(item.resources?.combat_value_ratio)||0));$('#run_chart').innerHTML=records.map((item,index)=>{const ratio=Number(item.resources?.combat_value_ratio)||0;return `<div class="run-column ${item.won?'win':''}" data-attempt="${esc(item.attempt_id)}"><span class="run-tip">${esc(item.map)} / ${esc(item.opponent)} / ratio ${ratio.toFixed(2)}</span><i class="run-bar" style="height:${12+clamp(ratio/maxRatio*88)}%"></i><span class="run-label">${index+1}</span></div>`}).join('')}else $('#run_chart').innerHTML=empty('No learning curve yet','Run autonomous evaluations to create the first attempt.');const difficulties=data.by_difficulty||{};$('#difficulty_grid').innerHTML=Object.keys(difficulties).length?Object.entries(difficulties).map(([name,value])=>`<div class="difficulty"><span>${esc(name)}</span><b>${num(value.win_rate)}%</b><small>${num(value.wins)} / ${num(value.attempts)} wins</small><div class="meter"><i style="width:${clamp(value.win_rate)}%"></i></div></div>`).join(''):empty('No difficulty data','Difficulty performance appears after recorded matches.');$('#learning_lessons').innerHTML=assessmentHtml(data.latest_lessons||{});const candidates=Object.values(data.policies?.candidates||{});$('#policy_gates').innerHTML=candidates.length?candidates.map(candidate=>{const evaluation=candidate.evaluation||{};return `<div class="goal"><i class="goal-dot"></i><div><b>${esc(candidate.candidate_id)}</b><small>${esc(candidate.rationale||'Awaiting evaluation')}</small></div><span class="badge ${evaluation.passed?'good':'warn'}">${evaluation.passed?'PROMOTED':`${num(evaluation.games)} GAMES`}</span></div>`}).join(''):`<div class="goal"><i class="goal-dot"></i><div><b>Baseline policy active</b><small>A candidate needs at least three games, material win-rate improvement, and zero safety violations.</small></div><span class="badge good">SAFE</span></div>`;$('#attempt_table').innerHTML=records.length?`<table><thead><tr><th>Result</th><th>Map</th><th>Difficulty</th><th>Combat ratio</th><th>Buildings</th><th>Losses</th><th>Policy</th></tr></thead><tbody>${[...records].reverse().map(item=>{const military=finalMilitary(item);return `<tr data-attempt="${esc(item.attempt_id)}"><td class="result ${item.won?'win':'loss'}">${item.won?'WIN':cap(item.result)}</td><td>${esc(item.map)}</td><td>${esc(cap(item.opponent))}</td><td>${Number(item.resources?.combat_value_ratio||0).toFixed(2)}</td><td>${num(military.buildings_killed)}</td><td>${num(military.units_lost)}</td><td>${esc(item.policy_id||'baseline')}</td></tr>`}).join('')}</tbody></table>`:empty('No comparisons available','Recorded attempts will appear here.')}
function renderBrain(){const brain=app.state.brain||{},controller=brain.controller||{},program=controller.strategy_program||{},goals=brain.goals||{};$('#brain_owner').textContent=`OWNER: ${cap(brain.owner||'unknown')}`;$('#brain_cards').innerHTML=[['Current owner',cap(brain.owner||'waiting'),'The layer authorized to produce the next bounded action.'],['Native profile',cap(program.profile||app.health.strategy?.active_native_profile||'unknown'),program.intent||'No live strategy program.'],['Fast decision',cap(controller.next_fast_decision?.key||'none'),controller.next_fast_decision?.summary||'No urgent safety action is queued.']].map(item=>`<article class="brain-card"><div class="card-kicker">Control state</div><b>${esc(item[1])}</b><p>${esc(item[2])}</p></article>`).join('');const active=goals.active||[],recent=(goals.recent||[]).slice(-6).reverse();$('#goals').innerHTML=(active.length?active:recent).length?(active.length?active:recent).map(goal=>`<div class="goal"><i class="goal-dot"></i><div><b>${esc(goal.summary||goal.instruction||'Goal')}</b><small>${esc(cap(goal.scope||goal.owner))} / ${esc(goal.last_error||'snapshot verification')}</small></div><span class="badge ${goal.status==='succeeded'?'good':goal.status==='failed'?'danger':''}">${esc(cap(goal.status||'active'))}</span></div>`).join(''):empty('No goals recorded','Goals appear as actions are proposed, executed, and verified against later snapshots.');const leases=brain.leases||[],updates=brain.latest_goal_updates||[];$('#leases').innerHTML=leases.length?leases.map(lease=>`<div class="goal"><i class="goal-dot"></i><div><b>${esc(cap(lease.owner))}</b><small>${esc(cap(lease.scope))}</small></div><span class="badge">LEASE</span></div>`).join(''):updates.length?updates.map(update=>`<div class="goal"><i class="goal-dot"></i><div><b>${esc(update.summary||update.instruction||'Receipt')}</b><small>${esc(update.last_error||'Verified from snapshot')}</small></div><span class="badge ${update.status==='succeeded'?'good':''}">${esc(cap(update.status))}</span></div>`).join(''):empty('No active leases','The command surface is currently uncontested.');$('#brain_raw').textContent=JSON.stringify(brain,null,2)}
function renderSettings(){const config=app.state.config||{};fields.forEach(key=>{const node=$('#'+key);if(node&&config[key]!==undefined)node.value=config[key]})}
function renderAll(){renderShell();renderLive();renderReview();renderLearning();renderBrain();renderSettings();bindDynamic()}
function bindDynamic(){$$('[data-tick]').forEach(node=>node.onclick=()=>selectFrameNear(Number(node.dataset.tick)||0));$$('[data-frame]').forEach(node=>node.onclick=()=>{app.frameIndex=Number(node.dataset.frame);frameViewer(selectedRecord(),app.tab==='review'?'#review_frame':'#live_frame');bindDynamic()});$$('[data-attempt]').forEach(node=>node.onclick=()=>selectAttempt(node.dataset.attempt))}
function setTab(tab){app.tab=tab;$$('.nav-button').forEach(button=>button.classList.toggle('active',button.dataset.tab===tab));$$('.view').forEach(view=>view.classList.toggle('active',view.id===`view-${tab}`));$('#crumb').textContent={live:'LIVE COMMAND',review:'AFTER ACTION',learning:'LEARNING LAB',brain:'BRAIN INSPECTOR',settings:'SETTINGS'}[tab]||cap(tab);renderAll()}
function selectAttempt(id){const match=app.matches.find(item=>item.attempt_id===id);if(!match)return;app.selected=match;app.frameIndex=-1;setTab('review')}
async function loadAll(){const button=$('#refresh');button.disabled=true;try{const [health,state,dashboard]=await Promise.all([request('/health'),request('/v1/state'),request('/v1/learning')]);app.health=health;app.state=state;app.dashboard=dashboard;const recent=dashboard.recent_attempts||[];app.matches=(await Promise.all(recent.map(item=>request(`/v1/learning/matches/${encodeURIComponent(item.attempt_id)}`).catch(()=>item)))).filter(Boolean);app.latest=app.matches[0]||((dashboard.latest_attempt&&await request('/v1/learning/latest').catch(()=>dashboard.latest_attempt))||{});if(!app.selected&&app.latest?.attempt_id)app.selected=app.latest;else if(app.selected){app.selected=app.matches.find(item=>item.attempt_id===app.selected.attempt_id)||app.latest}app.lastSync=new Date();renderAll()}catch(error){toast(`War Room refresh failed: ${error.message}`);$('#router_status').className='status-pill offline';$('#router_status').querySelector('span').textContent='Control plane unavailable'}finally{button.disabled=false}}
async function loadLive(){try{const [health,state]=await Promise.all([request('/health'),request('/v1/state')]);app.health=health;app.state=state;app.lastSync=new Date();renderShell();renderLive();renderBrain();bindDynamic()}catch{}}
$$('[data-tab]').forEach(button=>button.onclick=()=>setTab(button.dataset.tab));$('#refresh').onclick=loadAll;$('#attempt_select').onchange=event=>selectAttempt(event.target.value);$('#reload').onclick=loadAll;$('#save').onclick=async()=>{try{const body=Object.fromEntries(fields.map(key=>[key,$('#'+key).value]));await request('/v1/config',{method:'POST',body:JSON.stringify(body)});toast('Configuration saved locally.');await loadAll()}catch(error){toast(`Save failed: ${error.message}`)}};$$('[data-test]').forEach(button=>button.onclick=async()=>{const test=button.dataset.test;button.disabled=true;$('#diag_output').textContent=test==='microphone'?'Listening for three seconds - speak now...':`Running ${test} test...`;try{$('#diag_output').textContent=JSON.stringify(await request('/v1/test/'+test,{method:'POST',body:'{}'}),null,2);await loadLive()}catch(error){$('#diag_output').textContent=`${test} failed: ${error.message}`}finally{button.disabled=false}});
loadAll();setInterval(loadLive,5000);
</script>
</body>
</html>"""
