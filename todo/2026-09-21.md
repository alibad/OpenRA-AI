# 2026-09-21

## Repeatable Mac voice input and one dependable installation

- Holding the configured Ask shortcut while an answer is still active now
  interrupts that answer and immediately starts the next recording. Players no
  longer lose a press and wonder whether voice input has stopped working.
- Whisper silence markers such as `[BLANK_AUDIO]` are treated as no speech,
  with a clear retry state instead of being submitted to the assistant as if
  the player had spoken a real question.
- Regression coverage exercises repeated push-to-talk replacement, including
  replacement of a blocked prior request, and blank-audio handling. The full
  companion suite passes 256 tests with two
  platform-specific skips. A newly synchronized Windows content-discovery test
  was isolated from game files already installed on the release Mac so the
  suite remains deterministic.
- Built `0.1.0-alpha.18-ra2-preview.12` against product main and engine
  `dde7f13cb1d432a98aa121a5fd8826ff3842bcf0`. The native RA2 build completed
  with zero warnings and zero errors. Apple notarization submission
  `b87b6271-1044-4ec7-8bc7-0cd8f1d1fcbd` was Accepted; the signed DMG is
  stapled and has SHA-256
  `67d3fc755bd7053a6ed5fd054df3a05a8ddc5233f08fd2e3c04f4949f6e801b9`.
- Checksum, strict nested signatures, Gatekeeper, packaged microphone
  discovery and the read-only mounted-DMG smoke test pass. The installed
  companion reached control readiness, loaded the local AI pack, reported all
  voice capabilities ready, and accepted three consecutive start/stop cycles.
- Installed the exact verified payload as the single canonical
  `/Applications/OpenRA AI.app`. The five former Applications copies were
  moved intact to a recoverable, non-release backup under
  `artifacts/installed-app-backups/2026-09-21-before-preview11`; settings, saves,
  owned RA2 content and model data were not changed.
- Live spoken-word recognition still requires the player to say a phrase into
  the running app; automated release validation can prove microphone access
  and repeatable capture lifecycle, but cannot manufacture a genuine human
  voice sample.
