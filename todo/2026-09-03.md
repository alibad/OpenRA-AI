# 2026-09-03

## RA2 modern faction prototype

- Added independently selectable China, Iran and Turkey packs to real RA2,
  retaining its original nine countries. Each has four signature units on a
  complete native Allied/Soviet economy, infantry and naval tree.
- Fresh profiles use the Modern Factions preset with native medium starting
  forces, so the selected country's signature tank is visible immediately.
  Existing saved/custom presets are preserved; Original Countries remains
  available from Change Experience.
- Reused the authored faction geometry and art lessons: 21 native RA2 voxel
  models with separate turrets, custom RA2 remap palette, native lighting and
  shadows, drone crash models, muzzle effects and factory roof animations.
  Generated twelve original sidebar portraits with the built-in image tool.
- Reviewed real GPU-rendered captures alongside stock Grizzly/Rhino/IFV/GI
  references. Corrected scale, team-color coverage, bright-neutral materials,
  the RA2 normal/palette contract, power-of-two UI atlas sizing and sidebar
  faction mappings before packaging. Art sources/prompts are committed under
  `assets/ra2-modern-factions/`.
- All twelve units passed real native production, foreign-faction exclusion
  and attack-role checks; each country's signature tank passed exact-cell
  movement. These bounded smoke tests use a
  private prebuilt economy and fast-build, not an all-tech cheat; they are
  separate from normal-speed AUTO tests starting with only an MCV.
- Longer AUTO tests reproduced excessive discretionary expansion before the
  first war factory. Added an opt-in ordered opening to the native bot, tested
  independently and enabled only by the modern preset. Legacy profiles retain
  their existing behavior. Normal-speed validation continues below.
- Verification at source stage: 519 engine tests passed (two existing skips),
  207 companion tests passed, and RA2 rules/all-map lint passed with its 793
  existing upstream warnings and no new errors.
- This remains a local prototype, not an official Windows/public release or a
  port of every World War III faction/ability. No website or GitHub Actions
  changes. Final Mac artifact/signature/install evidence is recorded separately.

### Final graphics review

- The four-facing review exposed clipped armor highlights under RA2's stock
  overbright lighting. Bounded the light multiplier for custom vehicles,
  drones and crash husks, leaving original RA2 models untouched. Added a
  regression test and reviewed another genuine in-game capture. Preview.6 was
  an intermediate candidate; the corrected candidate is preview.7.

### Final prototype verification

- 208 companion tests passed after the lighting regression; the engine remains
  at 519 passing tests and two existing skips. Modern RA2 rules/all-map lint
  reports 793 existing warnings and no errors. The Original Countries preset
  also passes, with 826 warnings (33 additional unused modern localization
  keys while those packs are disabled).
- All 27 playable maps passed startup/AUTO deployment at tick 150. Repeated
  three startup checks against the signed preview.7 payload successfully.
- All twelve units passed native factory production and weapon/target-role
  checks again in the packaged payload, with foreign-country units excluded.
  All three signature tanks reached the requested cell. The private prebuilt
  economy uses fast-build, normal costs and prerequisites, not all-tech.
- Normal-speed MCV-only AUTO built factories and recruited new units: Turkey
  produced Bozkir by tick 5590; China produced Mantis and Iran produced Fajr
  within the longer run. The stricter Qilin/Karrar-specific tests timed out at
  tick 10340 (seven minutes wall time); their original failing result files are
  retained. This is not a guarantee of early signature-tank production. AUTO
  army composition/economy pacing remains a tuning item.

### Signed Mac preview.7 installed

- Built `0.1.0-alpha.18-ra2-preview.7` from product
  `e59047a8d2f2c4f1489251081a106be03b97569c`, with the clean engine pinned to
  `984794cb9d65046cff29511b6b45eff7da3c4129`. The faction implementation is
  `c4e227c33c41da8f8c1ea5ce85576f70137c0461`; the engine palette/UI fix is
  `d718353550001e98d836d2fa1617ff66babc3bd9`. All are on their repository main.
- Artifact: `artifacts/releases/OpenRA-AI-0.1.0-alpha.18-ra2-preview.7-macos-arm64.dmg`.
  Final stapled-DMG SHA-256:
  `34ff5393bfb5479b1244a5e8fe260ccf1703c785a359432dacf8c88397268f1c`.
- Signed by `Developer ID Application: HUMAN QUEST LLC (75E8FZ9444)`.
  Apple notarization `69178547-825f-4e5b-84bf-89cb4c89daa1` was Accepted with
  no issues. DMG/app signature verification, stapled-ticket validation,
  Gatekeeper assessment and the mounted-DMG smoke script passed. Evidence:
  `artifacts/ra2-modern/notary-preview7.json` and `smoke-preview7.log`.
- Installed from the verified read-only DMG to
  `/Applications/OpenRA AI RA2 Preview.app`. Preserved preview.5 at
  `artifacts/ra2-modern/superseded-preview.5.app`; other Applications entries
  and owned game content were not changed.
- Launched the installed wrapper into RA2 without starting a match. Its live
  health endpoint reports preview.7, `control_ready: true`, AI enabled and AUTO
  enabled; the bundled local-model and Whisper servers start with it. Native
  UI automation could not attach to this multi-process app, so final menu
  interaction was not re-verified through that tool. Packaged gameplay was
  independently verified with disposable profiles as described above.
- Existing skirmish settings still select MCV-only starts. To see a new tank
  immediately, choose **Skirmish → China / Iran / Turkey** and set starting
  units to **Medium**. Existing personal settings were preserved.
- Local Mac preview only: no public release URL, website deployment or Windows
  installer was produced. Voice-key interception, local free-form AI reliability,
  long-session balance and multiplayer remain separate validation items.

## What we built

- An optional native Red Alert 2 preview for Apple Silicon Macs, using the
  player's owned Steam artwork instead of a paid Windows compatibility trial.
- Real RA2 countries, isometric terrain, voxel vehicles, Allied/Soviet UI and
  base building through the OpenRA RA2 mod, rather than describing an
  RA2-inspired capability pack as the full game.
- A separate Applications entry and private settings/content/save directory,
  keeping the working OpenRA AI installation intact.
- A repeatable pinned-source build with verified downloads, safe game-data
  import, explicit version information and no bundled proprietary game files.

## What we fixed

- Adapted the older RA2 mod to our current engine, including resource docking,
  voxel previews/rendering, synchronization fields, map formats and lobby setup.
- Fixed crashes opening settings, starting skirmishes and viewing the in-game
  menu; checked country selection, a fresh human-controlled match, MCV
  deployment and successful power-plant production/placement.
- Prevented incomplete AI settings from appearing functional in this preview.
- Preserved macOS signatures when copying the preview into Applications.

## Key decisions

- Keep the engine submodule pinned; never silently switch it to the upstream
  mod's older engine or overwrite another session's work.
- Clearly identify this as a local RA2 skirmish preview, not an official
  release or a feature-complete reproduction. Companion, voice, AUTO,
  original campaigns and Yuri's Revenge still need separate integration.
- No CrossOver, no GitHub Actions, no public release or website changes.
- Validation includes the native build, all included map/rules checks,
  safe-import regression tests and interactive Mac smoke testing. Upstream
  localization warnings remain; ad-hoc signatures are not notarization.
- Verified 28 maps, 10 preview regression tests, and 508 engine tests
  (2 pre-existing skips). The isolated engine test run needed the source
  fixtures linked into its output layout; the corrected run passed.
- The native builds have no compiler warnings or errors. RA2 rules validation
  reports 793 upstream warnings, primarily missing or unused translations.

## Breaking changes

- None for the existing OpenRA AI application or public releases.

## Integrated RA2 and shared assistant follow-up

- The Mac packager now includes RA2 alongside World War III in one OpenRA AI
  app, with a remembered native game selector and shared assistant/model settings.
- RA2 uses its own unit names and isometric bridge coordinates. Native AUTO
  profiles deploy the MCV and handle real-time economy, production and combat.
- The packaged launcher still waits for companion health before opening the
  game; cleanup now follows child processes instead of only the launcher PID.
- Owned Steam/preview RA2 data can be imported into the shared library without
  overwriting different files or bundling proprietary archives.
- Fixed fresh-profile RA2 map-folder resolution and off-map prison fences that
  caused frozen-under-fog startup crashes. All 27 playable maps passed isolated
  headless startup/AUTO deployment checks.
- Shared voice labels and the always-visible, configured Ask shortcut are now
  included in the RA2 HUD. Local Whisper readiness was confirmed on this Mac;
  end-to-end physical shortcut/audio verification is still a release gate.
- Verification: 191 companion tests, 513 engine tests (two existing skips),
  shell syntax checks and whitespace validation passed. Live Soviet AUTO built
  power and a refinery and continued production.
- The five custom factions remain intact in World War III. Porting their
  distinct rosters/abilities to RA2 is not complete; asset reuse needs a product
  decision. Original campaigns and Yuri's Revenge remain outside this preview.
- No public release, website change, Windows RA2 build or GitHub Actions change
  is implied by this integration commit. Mac packaging validation follows below.

## Longer RA2 match validation

- The first Developer-ID signed/notarized integrated build exposed an Allied
  GI crash during live play and was deliberately not installed. The pinned
  engine now handles RA2 units with multiple conditional weapon modes.
- All 27 playable maps passed startup/AUTO checks with the corrected engine.
  An Allied Heartland match reached tick 4730, produced a GI, accepted its
  deployment order and reported the correct range change from 4 to 5 cells.
- Isometric manual movement also passed: the MCV reached the exact requested
  map-storage cell. These checks use disposable profiles, not player saves.
- Verification passes 514 engine tests (two existing skips), 191 companion
  tests, shell syntax and whitespace checks. RA2 bot names now use translation
  keys to avoid five integration-introduced lint warnings.
- This correction requires a new Mac preview build. Other longer match checks,
  final packaged-app validation and local installation are recorded separately.

## Final preview usability follow-up

- Country/game questions and questions about power plants no longer trigger
  an unrelated canned strategy explanation. Added explicit regression cases.
- The RA2 start message now identifies its native countries and shared AI
  controls, while clearly locating custom faction packs in World War III.
- Both Allied long runs passed GI production/deployment (ticks 4730 and 6680).
  A Soviet run stayed active through tick 6240 but did not produce a Conscript
  within 260 seconds; native AUTO production pacing remains a known limitation.
- The second signed/notarized preview showed AUTO already deploying/building
  at tick 200. A live local-model question correctly listed the player's
  buildings in 782 ms. Whisper readiness passed, but physical voice input is
  not verified: Superwhisper appears to intercept the default shortcut.
- Latest verification: 515 engine tests (two existing skips), 192 companion
  tests, 27 map-start checks, movement and two GI deployment checks. The RA2
  package passes rules validation with the original 793 upstream warnings.
- The launcher cleanup test left no game or companion process after exit.
  A final replacement package is needed for these last usability corrections.

## Installed local Mac preview

- Installed `0.1.0-alpha.18-ra2-preview.3` as **OpenRA AI RA2 Preview**, preserving
  all older Applications entries. It comes from product `6e07985` and clean
  engine `c84262d9b2`, with the GI crash fix and clarified shared-AI UI.
- Apple accepted notarization; the DMG and installed app are stapled, their
  signatures/checksum validate, and Gatekeeper accepts the installed copy.
- The final signed package passed three additional map/AUTO startup checks.
  Microphone device availability passed, but physical voice input is still
  unverified because of the apparent Superwhisper shortcut conflict.
- This is not the complete requested experience: RA2 custom faction ports,
  assistant tool-subprocess packaging, reliable natural-language orders,
  country-answer accuracy and AUTO production pacing remain follow-up work.
- Full artifact hashes, commits, tests and limitations are recorded in
  `docs/ra2-mac-preview-validation.md`. Nothing was published to the website
  or as an official Windows/Mac release, and no GitHub Actions were changed.

## Normal Applications launch and packaged game tools

- First launch from Applications exposed a macOS Bash compatibility error
  with an empty argument list. Normal double-click launches now construct the
  selected game's arguments safely. The regression test runs the real system
  Bash with no arguments, a remembered RA2 selection, and spaced map arguments.
- Added an explicit bundled game-tool service entry point. The frozen companion
  no longer tries to run itself as a Python interpreter when connecting its
  assistant tools. Packaging now smoke-tests that entry point after signing.
- These corrections supersede preview.3; final installation and updated
  notarization/artifact evidence follow validation of the replacement build.

## Prompt grounding and menu-to-match readiness

- Capped local game-bridge reconnect backoff at one second, so time spent in
  the main menu no longer turns into a long AI connection delay at match start.
- The assistant now explicitly reads a bounded live battlefield before asking
  the local model to answer. RA2 counts/production use the active game's actor
  names, and bundled profiles allow 8192 tokens for the game-tool instructions.
- A live tool-connected local answer correctly identified Germany and the
  owned buildings in 1384 ms. All 199 companion tests passed, including new
  startup-backoff, context-size, faction grounding and RA2 label regressions.
- Natural-language action quality is still a preview limitation: a local-model
  power-plant request produced malformed repetitive output and no accepted
  action. Tool-backed read-only answers and native AUTO are separate checks;
  this is not evidence that arbitrary spoken/text commands are release-ready.

## Validated replacement installed — preview.5

- Built product `d87bf56` / engine `c84262d9b2` as
  `0.1.0-alpha.18-ra2-preview.5`. Apple accepted notarization; DMG checksum,
  signature, stapled tickets and installed-app Gatekeeper checks all passed.
- Replaced only **OpenRA AI RA2 Preview** in Applications, retaining preview.3
  as a recoverable artifact backup and preserving all other apps/game content.
- The actual packaged companion connected 1.453 seconds after match-bridge
  startup (tick 20) following a long menu wait. AUTO was on immediately and
  deployed the MCV in the opening seconds. The HUD shows Option + Space.
- A packaged tool-backed question correctly identified Iraq and its owned
  Construction Yard in 3578 ms. All 28 game tools were available; local
  Whisper readiness and microphone-device detection passed.
- Three final signed-map startup checks passed. RA2 rules validation passed
  with the original 793 warnings. Implementation verification remains 199
  companion tests and 515 engine tests, with two existing engine skips.
- This remains a local preview: custom modern factions still live in World
  War III, physical voice/accept/reject input and reliable free-form orders
  remain release gates, and Soviet AUTO production pacing needs tuning.
  No public assets, website deployment, Windows build or Actions were changed.
