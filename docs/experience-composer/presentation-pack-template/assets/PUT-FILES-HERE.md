Place `mouse.shp` here to match the example `Replaces` entry in `pack.yaml`.

Delete this instruction file before installing the pack. Documentation and other unsupported file types are intentionally rejected inside an installed pack's `assets` folder.
