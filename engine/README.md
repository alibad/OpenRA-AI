# Engine

`engine/openra` is reserved for a pinned submodule targeting:

```text
https://github.com/alibad/OpenRA.git
```

Engine-level C# changes belong in that fork. This repository records the exact
engine commit required by each OpenRA AI release.
