# 03: Straits Shield

Construction-enabled Turkey mission over Shuriken Island's native OpenRA
terrain. The deterministic packer imports only the base `map.bin` terrain and
preview, then writes this mission's own actors, rules, script, briefing, and
manifest into `straits-shield-2026.oramap`.
